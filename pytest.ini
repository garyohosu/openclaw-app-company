[pytest]
testpaths = tests
pythonpath = scripts
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: mark test as requiring real external services (CGI, HTTP)
    e2e: mark test as end-to-end requiring browser
