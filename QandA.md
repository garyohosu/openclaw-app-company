# QandA.md — SPEC.md レビュー質問集

作成日: 2026-03-23
対象: SPEC.md v0.1

---

## Q1. OpenClaw とは何か

**該当箇所:** セクション1、11.2
**質問:**
「OpenClaw」がどのようなシステムかが不明確です。

- 既存の外部ツール・フレームワークですか？それとも本プロジェクト独自の仕組みですか？
- エージェント同士の通信はどのように行われますか？（例：同一Claude会話内のターン管理、マルチエージェントフレームワーク、独自スクリプト など）
- `agents/agents.yaml` のスキーマ定義はありますか？Phase 0 の成果物として挙げられていますが、フォーマットが未定義です。

---

## Q2. Codex CLI の具体的な利用方法

**該当箇所:** セクション11.1、11.3、11.4
**質問:**
- 使用する Codex CLI は OpenAI の `codex` コマンドですか？バージョン・モデルの指定はありますか？
- 委任文（プロンプト）はどのように渡しますか？（例：`codex "プロンプト文"` として直接渡す、ファイルを読ませる など）
- Codex CLI が出力したコードの確認・採否は誰が（どのエージェントが）行いますか？

---

## Q3. Sakura CGI API Toolbox のベース URL

**該当箇所:** セクション8
**質問:**
- 各 CGI（`now.cgi`、`uuid.cgi` など）のベース URL がSPEC.md に記載されていません。`api.js` や `test-api.html` で使用するエンドポイントの URL はどこで管理しますか？
- 環境ごと（開発・本番）に URL が変わりますか？

---

## Q4. `db.cgi` の認証・認可

**該当箇所:** セクション10.3
**質問:**
- 現在 `db.cgi` に認証（APIキー・トークン等）はありますか？
  - もし未実装であれば、エンドポイント URL を知っている誰でも INSERT/DELETE が可能になりますが、それは許容範囲ですか？
- セクション10.3 に「APIキーやトークンが必要な機能は別途追加設計する」とありますが、MVP段階での扱いはどうなりますか？
- 許可される DB 名・テーブル名のホワイトリストは `db.cgi` 側で管理しますか？それともフロント側の制約ですか？

---

## Q5. `create_table` オペレーションの仕様

**該当箇所:** セクション9.6、10.4
**質問:**
- `db.cgi` の `operation` に `create_table` が含まれていますが、JSON 例が示されていません。
  - スキーマ（カラム定義、型など）はどのように指定しますか？
  - 既存テーブルが存在する場合はエラーになりますか、それとも `IF NOT EXISTS` 相当の動作ですか？

---

## Q6. `state/` ディレクトリがリポジトリ構成から欠落

**該当箇所:** セクション5、14（Phase 0）、20
**質問:**
- セクション20で `state/company_state.json` が定義されていますが、セクション5のリポジトリ構成ツリーに `state/` ディレクトリが含まれていません。
  - `state/` ディレクトリはリポジトリに含めますか？
  - `company_state.json` は Git で管理しますか（コミット対象）？それとも `.gitignore` に含めますか？

---

## Q7. 各フェーズのトリガーと進行管理

**該当箇所:** セクション14
**質問:**
- Phase 0〜11 は誰がトリガーしますか？
  - 人間が手動で各フェーズを開始しますか？
  - それとも OpenClaw が前フェーズの完了を検知して自動的に次フェーズへ進みますか？
- フェーズが失敗した場合のロールバック手順はありますか？

---

## Q8. `deploy.yml` の内容と GitHub Pages のトリガー

**該当箇所:** セクション5（`.github/workflows/deploy.yml`）
**質問:**
- `deploy.yml` はすでに存在しますか？存在しない場合、誰が（どのエージェントが）作成しますか？
- `main` ブランチへのプッシュで自動デプロイされますか？それとも手動トリガーですか？
- ルートの `index.html`（アプリ一覧）はビルドスクリプト（`build-index.py`）で生成しますか？それとも手動で管理しますか？

---

## Q9. `scripts/` 内スクリプトは既存か未作成か

**該当箇所:** セクション5
**質問:**
- `scripts/create-app-template.py` と `scripts/build-index.py` は既に存在しますか？
  - 未作成の場合、MVP に含めますか？
  - 含める場合、誰が（どのエージェントが）実装しますか？

---

## Q10. `validate.cgi` と `convert.cgi` の対応範囲

**該当箇所:** セクション9.3、9.4
**質問:**
- `validate.cgi` はどの JSON Schema バージョンを想定していますか？（Draft-07、Draft 2019-09 など）
- `convert.cgi` が対応する変換の種類・単位の一覧はありますか？アプリ設計時に必要になります。

---

## Q11. `visitor.cgi` のデータ構造

**該当箇所:** セクション9.5、15.4
**質問:**
- `visitor.cgi` に送る `visit` リクエストと、`stats` レスポンスの JSON 構造を教えてください。
- 訪問者データはどこに保存されますか？（SQLite？テキストファイル？）

---

## Q12. エージェント間の成果物受け渡し方法

**該当箇所:** セクション12〜13
**質問:**
- 例えば PRD Writer が書いた `artifacts/prd/prd.md` を Task Breakdown Agent が読む際、どのように受け渡しますか？
  - ファイルシステム経由（Git リポジトリ内ファイルを参照）ですか？
  - OpenClaw が内部で受け渡しますか？
- 複数エージェントが並行して作業する際の競合管理はどうしますか？

---

## Q13. セキュリティ審査の担当エージェント

**該当箇所:** セクション19
**質問:**
- 「危険な更新系操作には設計審査を通す」とありますが、具体的にどのエージェントが審査しますか？
- 審査の合否はどのように記録・管理しますか？（ADR？ それとも別の成果物？）

---

## Q14. `shared/` ディレクトリの利用方針

**該当箇所:** セクション5（`shared/components`、`shared/styles`、`shared/utils`）
**質問:**
- `shared/` 内のコンポーネント・スタイル・ユーティリティを各アプリが利用する場合、GitHub Pages のサブパス制約（相対パス問題）はどう解決しますか？
- 現時点で `shared/` に入れるものは決まっていますか？

---

## Q15. アプリ番号の採番ルール

**該当箇所:** セクション5（`app-001-sample`）、18.2
**質問:**
- `app-xxx-name` の `xxx` は連番ですか？それとも別の採番ルールがありますか？
- 廃止・削除されたアプリの番号は欠番にしますか？

---

_以上15件。回答が出た項目から順次 SPEC.md または設計ドキュメントへ反映してください。_
